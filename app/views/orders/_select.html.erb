<%= form_with url: orders_path, method: :get, local: false do |f| %>
  <%= f.select :order, [["新着順", "new"],["高評価＆コメントの多い順", "high_rate"],["いいねの多い順", "many_favorites"]],
  include_blank: "並び替え" %>
  <%= f.submit "▼" %>
<% end %>
