<div class="row justify-content-center mb-3">
  <div class="col text-center">
    <h5>マイお気に入り<strong>穴場リストページ</strong></h5>
  </div>
</div>
<div class="row">
  <% @favorite_lists.each do |favorite_list| %>
  <div class=" col-4 col-md-3 col-lg-2 mb-1 pl-1">
    <%= link_to post_path(favorite_list.id) do %>
      <%= image_tag favorite_list.get_image, size: "118x118", class: "rounded" %>
    <% end %>
  </div>
  <% end %>
</div>
